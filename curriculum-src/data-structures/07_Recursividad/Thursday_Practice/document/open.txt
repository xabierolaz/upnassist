# Abrir un selector de archivos con un nombre para la ventana
nombre = easygui.fileopenbox(title="Verificador de parntesis")
# Intenta ver cmo se comporta el nombre del archivo
# Qu pasa si el usuario no elige un archivo?
print(nombre)

# Abrir un selector de archivos que solo muestra archivos .txt
nombre = easygui.fileopenbox(default=["*.txt"])
# Podemos limitar a otros tipos de archivos?
# Debera estar balanceado un archivo fuente de Python?
