{
  "id": "ds-w02-sparse",
  "part": 15.2,
  "title": {
    "ENG": "Lab: Sparse Matrices (COO)",
    "CAS": "Práctica: Matrices Dispersas (COO)",
    "EUS": "Praktika: Matrize Dispertsuak (COO)"
  },
  "blocks": [
    {
      "type": "markdown",
      "content": {
        "ENG": "# Sparse Matrices: The Visual Guide\n\n## 1. The Problem: An Ocean of Zeros\n\nImagine a giant matrix (e.g., 1000x1000). If most of it is empty (zeros), storing it as a standard grid is a huge waste of memory. \n\n**👇 Interactive Demo:** Click on the grid below to add numbers. Watch how the **Dense Matrix** stores mostly nothing, while the **COO Lists** (right) only store what matters.",
        "CAS": "# Matrices Dispersas: Guía Visual\n\n## 1. El Problema: Un Océano de Ceros\n\nImagina una matriz gigante (ej. 1000x1000). Si la mayoría está vacía (ceros), guardarla como una cuadrícula normal es un desperdicio enorme.\n\n**👇 Demo Interactiva:** Haz clic en la cuadrícula para añadir números. Observa cómo la **Matriz Densa** guarda \"nada\", mientras que las **Listas COO** (derecha) solo guardan lo importante.",
        "EUS": "# Matrize Dispertsuak: Gida Bisuala\n\n## 1. Arazoa: Zeroen Ozeanoa\n\nImajinatu matrize erraldoi bat (adib. 1000x1000). Gehiena hutsik badago (zeroak), sareta arrunt gisa gordetzea memoria galtzea da.\n\n**👇 Demo Interaktiboa:** Egin klik saretan zenbakiak gehitzeko. Ikusi nola **Matrize Trinkoak** \"ezer\" gordetzen duen, eta **COO Zerrendek** (eskuinean) garrantzitsua dena bakarrik."
      }
    },
    {
      "type": "interactive-sparse-matrix"
    },
    {
      "type": "markdown",
      "content": {
        "ENG": "## 2. Memory: The Hidden Cost\n\nAs you saw above, the **Dense Format** always uses 25 units of memory (for a 5x5 grid), regardless of the data. The **COO Format** starts empty and grows only when you add numbers.\n\n### 🧠 Concept: `sys.getsizeof` vs. Reality\nIn Python, lists are complex objects. `sys.getsizeof(list)` only counts the container's shell, not the items inside. To measure the *real* memory cost of a matrix, we need a recursive function that counts everything.\n\n**Why COO?**\n*   **Dense:** Cost = Rows × Cols (Quadratic growth 🟥)\n*   **COO:** Cost = 3 × Non-Zeros (Linear growth 🟩)",
        "CAS": "## 2. Memoria: El Coste Oculto\n\nComo has visto arriba, el **Formato Denso** siempre usa 25 unidades de memoria (para 5x5), sin importar los datos. El **Formato COO** empieza vacío y solo crece cuando añades números.\n\n### 🧠 Concepto: `sys.getsizeof` vs. Realidad\nEn Python, las listas son objetos complejos. `sys.getsizeof(lista)` solo cuenta la \"cáscara\" del contenedor, no los elementos de dentro. Para medir el coste *real*, necesitamos una función recursiva.\n\n**¿Por qué COO?**\n*   **Denso:** Coste = Filas × Cols (Crecimiento Cuadrático 🟥)\n*   **COO:** Coste = 3 × No-Ceros (Crecimiento Lineal 🟩)",
        "EUS": "## 2. Memoria: Ezkutuko Kostua\n\nGoian ikusi duzun bezala, **Formatu Trinkoak** beti 25 memoria-unitate erabiltzen ditu (5x5erako), datuak edozein direla ere. **COO Formatua** hutsik hasten da eta zenbakiak gehitzean bakarrik hazten da.\n\n### 🧠 Kontzeptua: `sys.getsizeof` vs. Errealitatea\nPython-en, zerrendak objektu konplexuak dira. `sys.getsizeof(lista)`-k edukiontziaren \"azala\" bakarrik kontatzen du. *Benetako* kostua neurtzeko, funtzio errekurtsibo bat behar dugu.\n\n**Zergatik COO?**\n*   **Trinkoa:** Kostua = Errenkadak × Zutabeak (Hazkunde Koadratikoa 🟥)\n*   **COO:** Kostua = 3 × Ez-Zeroak (Hazkunde Lineala 🟩)"
      }
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex5",
      "title": { "ENG": "Exercise: Calculating Real Memory", "CAS": "Ejercicio: Calculando Memoria Real", "EUS": "Ariketa: Benetako Memoria Kalkulatzen" },
      "description": {
        "ENG": "Implement a recursive function to calculate the total memory size of a nested list structure. Standard `sys.getsizeof` is not enough!",
        "CAS": "Implementa una función recursiva para calcular el tamaño total en memoria de una lista anidada. ¡El `sys.getsizeof` estándar no basta!",
        "EUS": "Inplementatu funtzio errekurtsibo bat habiaratutako zerrenda baten memoria osoa kalkulatzeko. `sys.getsizeof` estandarra ez da nahikoa!"
      },
      "initialCode": {
        "ENG": "import sys\n\ndef list_memory_size(lst: list) -> int:\n    size = sys.getsizeof(lst)\n    for element in lst:\n        if isinstance(element, list):\n            # TODO: Recursive call\n            pass\n        else:\n            # TODO: Add element size\n            pass\n    return size",
        "CAS": "import sys\n\ndef list_memory_size(lst: list) -> int:\n    size = sys.getsizeof(lst)\n    for elemento in lst:\n        if isinstance(elemento, list):\n            # TODO: Llamada recursiva\n            pass\n        else:\n            # TODO: Sumar tamaño elemento\n            pass\n    return size",
        "EUS": "import sys\n\ndef list_memory_size(lst: list) -> int:\n    tamaina = sys.getsizeof(lst)\n    for elementua in lst:\n        if isinstance(elementua, list):\n            # TODO: Dei errekurtsiboa\n            pass\n        else:\n            # TODO: Gehitu elementuaren tamaina\n            pass\n    return tamaina"
      },
      "testCode": "import sys; assert list_memory_size([1, [2]]) > sys.getsizeof([1, [2]])"
    },
    {
      "type": "markdown",
      "content": {
        "ENG": "## 3. The 3 Pillars of COO\n\nTo build our Sparse Matrix library, we need to master 3 operations:\n\n1.  **Read/Write:** Getting data in and out.\n2.  **Conversion:** Transforming Grid ↔ Lists.\n3.  **Manipulation:** Searching and Adding values (The \"Radar\" logic).",
        "CAS": "## 3. Los 3 Pilares de COO\n\nPara construir nuestra librería de Matrices Dispersas, necesitamos dominar 3 operaciones:\n\n1.  **Lectura/Escritura:** Entrar y sacar datos.\n2.  **Conversión:** Transformar Rejilla ↔ Listas.\n3.  **Manipulación:** Buscar y Añadir valores (La lógica del \"Radar\").",
        "EUS": "## 3. COOren 3 Zutabeak\n\nGure Matrize Dispertsuen liburutegia eraikitzeko, 3 eragiketa menperatu behar ditugu:\n\n1.  **Irakurri/Idatzi:** Datuak sartu eta atera.\n2.  **Bihurketa:** Sareta ↔ Zerrendak eraldatu.\n3.  **Manipulazioa:** Balioak Bilatu eta Gehitu (\"Radar\" logika)."
      }
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex1",
      "title": { "ENG": "1. Input Matrix", "CAS": "1. Leer Matriz", "EUS": "1. Matrizea Irakurri" },
      "description": {
        "ENG": "Read a matrix from the console. Use a loop to read rows until an empty line is entered.",
        "CAS": "Lee una matriz desde la consola. Usa un bucle para leer filas hasta que se introduzca una línea vacía.",
        "EUS": "Irakurri matrize bat kontsolatik. Erabili begizta bat errenkadak irakurtzeko lerro huts bat sartu arte."
      },
      "initialCode": {
        "ENG": "def input_matrix() -> list:\n    matrix = []\n    print(\"Enter matrix (empty line to finish):\")\n    while True:\n        line = input()\n        if not line: \n            break\n        # 1. Split the line into strings\n        # 2. Convert each string to int and build a list (the row)\n        # 3. Append the row to the matrix\n    return matrix",
        "CAS": "def input_matrix() -> list:\n    matriz = []\n    print(\"Introduce la matriz (línea vacía para terminar):\")\n    while True:\n        linea = input()\n        if not linea: \n            break\n        # 1. Divide la linea en partes (split)\n        # 2. Convierte cada parte a int y crea una lista (la fila)\n        # 3. Añade la fila a la matriz\n    return matriz",
        "EUS": "def input_matrix() -> list:\n    matrizea = []\n    print(\"Sartu matrizea (lerro hutsa amaitzeko):\")\n    while True:\n        lerroa = input()\n        if not lerroa: \n            break\n        # 1. Zatitu lerroa (split)\n        # 2. Bihurtu zati bakoitza int-era eta sortu zerrenda bat (errenkada)\n        # 3. Gehitu errenkada matrizea-ri\n    return matrizea"
      },
      "testCode": "import unittest\n# Mock testing here"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex2",
      "title": { "ENG": "2. Read File", "CAS": "2. Leer Fichero", "EUS": "2. Fitxategia Irakurri" },
      "description": {
        "ENG": "Read a matrix from a text file. Use `with open(...)` and iterate through the file object.",
        "CAS": "Lee una matriz de un fichero de texto. Usa `with open(...)` y recorre el objeto fichero.",
        "EUS": "Irakurri matrize bat testu fitxategi batetik. Erabili `with open(...)` eta zeharkatu fitxategi-objektua."
      },
      "initialCode": {
        "ENG": "def read_matrix(path: str) -> list:\n    matrix = []\n    # with open(path, 'r') as f:\n    #     for line in f:\n    #         # Process the line and append to matrix\n    return matrix",
        "CAS": "def read_matrix(ruta: str) -> list:\n    matriz = []\n    # with open(ruta, 'r') as f:\n    #     for linea in f:\n    #         # Procesa la linea y añade a la matriz\n    return matriz",
        "EUS": "def read_matrix(bidea: str) -> list:\n    matrizea = []\n    # with open(bidea, 'r') as f:\n    #     for lerroa in f:\n    #         # Prozesatu lerroa eta gehitu matrizea-ri\n    return matrizea"
      },
      "testCode": "import unittest\n# Mock file testing"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex3",
      "title": { "ENG": "3. Initialize COO", "CAS": "3. Inicializar COO", "EUS": "3. COO Hasieratu" },
      "description": {
        "ENG": "Return an empty COO structure: a list containing 3 empty lists.",
        "CAS": "Devuelve una estructura COO vacía: una lista que contiene 3 listas vacías.",
        "EUS": "Itzuli COO egitura huts bat: 3 zerrenda huts dituen zerrenda bat."
      },
      "initialCode": {
        "ENG": "def create_coo() -> list:\n    # Return a list with 3 empty lists inside\n    return []",
        "CAS": "def crear_coo() -> list:\n    # Devuelve una lista con 3 listas vacías dentro\n    return []",
        "EUS": "def sortu_coo() -> list:\n    # Itzuli barruan 3 zerrenda huts dituen zerrenda bat\n    return []"
      },
      "testCode": "import unittest\nfunc = locals().get('crear_coo') or locals().get('create_coo') or locals().get('sortu_coo')\nassert func() == [[], [], []]"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex4",
      "title": { "ENG": "4. Dense to COO", "CAS": "4. Denso a COO", "EUS": "4. Trinkotik COOra" },
      "description": {
        "ENG": "Iterate the grid. If you find a non-zero value, save it to the 3 lists.",
        "CAS": "Recorre la rejilla. Si encuentras un valor no-cero, guárdalo en las 3 listas.",
        "EUS": "Iteratu sareta. Zero ez den balio bat aurkitzen baduzu, gorde 3 zerrendetan."
      },
      "initialCode": {
        "ENG": "def normal_to_coo(matrix: list) -> list:\n    coo = [[], [], []]\n    # Use nested loops (for i, for j)\n    # if matrix[i][j] != 0:\n    #     # Append value, row (i) and col (j)\n    return coo",
        "CAS": "def normal_a_coo(matriz: list) -> list:\n    coo = [[], [], []]\n    # Usa bucles anidados (for i, for j)\n    # si matriz[i][j] != 0:\n    #     # Añade valor, fila (i) y col (j)\n    return coo",
        "EUS": "def normal_to_coo(matrizea: list) -> list:\n    coo = [[], [], []]\n    # Erabili habiaratutako begiztak (for i, for j)\n    # matrizea[i][j] != 0 bada:\n    #     # Erantsi balioa, errenkada (i) eta zutabea (j)\n    return coo"
      },
      "testCode": "m = [[0,1],[2,0]]; func = locals().get('normal_a_coo') or locals().get('normal_to_coo'); assert func(m) == [[1,2],[0,1],[1,0]]"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex6",
      "title": { "ENG": "6. COO to Dense", "CAS": "6. COO a Denso", "EUS": "6. COOtik Trinkora" },
      "description": {
        "ENG": "Reconstruct the grid. Start with zeros, then fill in the values from the COO lists.",
        "CAS": "Reconstruye la rejilla. Empieza con ceros, luego rellena con los valores de las listas COO.",
        "EUS": "Berreraiki sareta. Hasi zeroekin, gero bete balioak COO zerrendetatik."
      },
      "initialCode": {
        "ENG": "def coo_to_normal(coo: list, rows: int, cols: int) -> list:\n    # 1. Create a matrix of rows x cols filled with 0s\n    # 2. Iterate through COO lists using range(len(coo[0]))\n    # 3. Place each value in the correct matrix[r][c]\n    return []",
        "CAS": "def coo_a_normal(coo: list, filas: int, columnas: int) -> list:\n    # 1. Crea una matriz de filas x columnas llena de 0s\n    # 2. Recorre las listas COO usando range(len(coo[0]))\n    # 3. Pon cada valor en su matriz[f][c] correcto\n    return []",
        "EUS": "def coo_to_normal(coo: list, errenkadak: int, zutabeak: int) -> list:\n    # 1. Sortu errenkada x zutabe matrize bat 0z beteta\n    # 2. Zeharkatu COO zerrendak range(len(coo[0])) erabiliz\n    # 3. Jarri balio bakoitza dagokion matrizea[e][z] posizioan\n    return []"
      },
      "testCode": "coo = [[1],[0],[1]]; func = locals().get('coo_a_normal') or locals().get('coo_to_normal'); assert func(coo, 1, 2) == [[0, 1]]"
    },
    {
      "type": "markdown",
      "content": {
        "ENG": "## 4. The \"Radar\" Algorithm\n\nNow comes the hardest part. How do we sum two COO matrices? We cannot just sum the lists index by index.\n\nWe need a **\"Radar\"**: a function that tells us if a coordinate `(r, c)` already exists in our COO lists.\n\n*   **If it exists:** We get the index so we can update the value.\n*   **If not:** We get `-1` so we know we must append a new entry.",
        "CAS": "## 4. El Algoritmo \"Radar\"\n\nAhora viene lo difícil. ¿Cómo sumamos dos matrices COO? No podemos sumar las listas índice por índice.\n\nNecesitamos un **\"Radar\"**: una función que nos diga si una coordenada `(f, c)` ya existe en nuestras listas COO.\n\n*   **Si existe:** Obtenemos el índice para poder sumar el valor.\n*   **Si no:** Obtenemos `-1` y sabemos que debemos añadir una entrada nueva.",
        "EUS": "## 4. \"Radar\" Algoritmoa\n\nOrain zailena dator. Nola batu bi COO matrize? Ezin ditugu zerrendak indizez indize batu.\n\n**\"Radar\"** bat behar dugu: `(e, z)` koordenatu bat gure COO zerrendetan existitzen den esaten digun funtzioa.\n\n*   **Existitzen bada:** Indizea lortzen dugu balioa eguneratzeko.\n*   **Ez bada:** `-1` lortzen dugu eta badakigu sarrera berri bat erantsi behar dugula."
      }
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex7",
      "title": { "ENG": "7. The Radar (is_it_in)", "CAS": "7. El Radar (existe_en)", "EUS": "7. Radarra (bertan_dago)" },
      "description": {
        "ENG": "Scan the `rows` and `cols` lists simultaneously. If you find a match for `(r, c)`, return the index. Else -1.",
        "CAS": "Escanea las listas `filas` y `cols` simultáneamente. Si encuentras coincidencia para `(f, c)`, devuelve el índice. Si no, -1.",
        "EUS": "Eskaneatu `errenkadak` eta `zutabeak` zerrendak aldi berean. `(e, z)`-rako bat-etortzea aurkitzen baduzu, itzuli indizea. Bestela -1."
      },
      "initialCode": {
        "ENG": "def is_it_in(coo: list, row: int, col: int) -> int:\n    # for k in range(len(coo[1])):\n    #     if coo[1][k] == row and coo[2][k] == col:\n    #         return k\n    return -1",
        "CAS": "def existe_en(coo: list, fila: int, columna: int) -> int:\n    # for k in range(len(coo[1])):\n    #     if coo[1][k] == fila and coo[2][k] == columna:\n    #         return k\n    return -1",
        "EUS": "def bertan_dago(coo: list, errenkada: int, zutabea: int) -> int:\n    # for k in range(len(coo[1])):\n    #     if coo[1][k] == errenkada and coo[2][k] == zutabea:\n    #         return k\n    return -1"
      },
      "testCode": "coo = [[9],[5],[3]]; func = locals().get('existe_en') or locals().get('is_it_in') or locals().get('bertan_dago'); assert func(coo, 5, 3) == 0; assert func(coo, 0, 0) == -1"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex8",
      "title": { "ENG": "8. Insert or Update", "CAS": "8. Insertar o Actualizar", "EUS": "8. Txertatu edo Eguneratu" },
      "description": {
        "ENG": "Use your Radar (`is_it_in`). If it finds the coordinate, add to the value. If not, append new data.",
        "CAS": "Usa tu Radar (`existe_en`). Si encuentra la coordenada, suma al valor. Si no, añade datos nuevos.",
        "EUS": "Erabili zure Radarra (`bertan_dago`). Koordenatua aurkitzen badu, gehitu balioari. Ez bada, erantsi datu berriak."
      },
      "initialCode": {
        "ENG": "def insert_or_increment(coo: list, row: int, col: int, value: int) -> None:\n    idx = is_it_in(coo, row, col) # Or existe_en\n    if idx != -1:\n        # Update: coo[0][idx] += value\n        pass\n    else:\n        # Append: Add value to coo[0], row to coo[1], col to coo[2]\n        pass",
        "CAS": "def insertar_o_incrementar(coo: list, fila: int, columna: int, valor: int) -> None:\n    idx = existe_en(coo, fila, columna) # O is_it_in\n    if idx != -1:\n        # Actualiza: coo[0][idx] += valor\n        pass\n    else:\n        # Añade: Add valor a coo[0], fila a coo[1], col a coo[2]\n        pass",
        "EUS": "def txertatu_edo_handitu(coo: list, errenkada: int, zutabea: int, balioa: int) -> None:\n    idx = bertan_dago(coo, errenkada, zutabea) # Edo is_it_in\n    if idx != -1:\n        # Eguneratu: coo[0][idx] += balioa\n        pass\n    else:\n        # Erantsi: Gehitu balioa coo[0]-ra, errenkada coo[1]-era, zutabea coo[2]-ra\n        pass"
      },
      "testCode": "coo = [[1],[0],[0]]; func = locals().get('insertar_o_incrementar') or locals().get('insert_or_increment') or locals().get('txertatu_edo_handitu'); radar = locals().get('existe_en') or locals().get('is_it_in') or locals().get('bertan_dago'); globals()['is_it_in'] = radar; globals()['existe_en'] = radar; func(coo,0,0,5); assert coo[0][0] == 6"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex9",
      "title": { "ENG": "9. The Merge (Sum)", "CAS": "9. La Fusión (Suma)", "EUS": "9. Bateratzea (Batuketa)" },
      "description": {
        "ENG": "To sum two matrices, create a NEW empty COO matrix. Then, pour ALL elements from Matrix 1 and ALL elements from Matrix 2 into it using `insert_or_increment`.",
        "CAS": "Para sumar dos matrices, crea una NUEVA matriz COO vacía. Luego, vierte TODOS los elementos de la Matriz 1 y TODOS los de la Matriz 2 en ella usando `insertar_o_incrementar`.",
        "EUS": "Bi matrize batzeko, sortu COO matrize huts BERRI bat. Gero, isuri Matrize 1eko elementu GUZTIAK eta Matrize 2ko elementu GUZTIAK bertara `txertatu_edo_handitu` erabiliz."
      },
      "initialCode": {
        "ENG": "def sum_sparses(coo1: list, coo2: list) -> list:\n    result = create_coo()\n    # 1. Loop through all indices of coo1 and insert into result\n    # 2. Loop through all indices of coo2 and insert into result\n    return result",
        "CAS": "def suma_dispersas(coo1: list, coo2: list) -> list:\n    resultado = crear_coo()\n    # 1. Recorre todos los elementos de coo1 e inserta en resultado\n    # 2. Recorre todos los elementos de coo2 e inserta en resultado\n    return resultado",
        "EUS": "def batu_dispertsuak(coo1: list, coo2: list) -> list:\n    emaitza = sortu_coo()\n    # 1. Zeharkatu coo1-eko elementu guztiak eta txertatu emaitzan\n    # 2. Zeharkatu coo2-ko elementu guztiak eta txertatu emaitzan\n    return emaitza"
      },
      "testCode": "import unittest\n# Complex sum testing"
    },
    {
      "type": "exercise",
      "exerciseId": "ds-w02-sparse-ex10",
      "title": { "ENG": "10. Pretty Print", "CAS": "10. Impresión Bonita", "EUS": "10. Inprimatze Polita" },
      "description": {
        "ENG": "Print the matrix so it looks like a grid (values separated by spaces).",
        "CAS": "Imprime la matriz para que parezca una rejilla (valores separados por espacios).",
        "EUS": "Inprimatu matrizea sareta bat bezala ikus dadin (balioak espazioz bereizita)."
      },
      "initialCode": {
        "ENG": "def print_matrix(matrix: list) -> None:\n    # for row in matrix:\n    #     # Print elements of the row separated by space\n    pass",
        "CAS": "def imprimir_matriz(matriz: list) -> None:\n    # for fila in matriz:\n    #     # Imprime elementos de la fila separados por espacio\n    pass",
        "EUS": "def inprimatu_matrizea(matrizea: list) -> None:\n    # for errenkada in matrizea:\n    #     # Inprimatu errenkadako elementuak espazioz bereizita\n    pass"
      },
      "testCode": "import unittest\n# Output capture testing"
    },
    {
      "type": "markdown",
      "content": {
        "ENG": "## 5. Final Challenge\n\nCombine everything: Read from file, read from console, convert both, sum them, and print the result!",
        "CAS": "## 5. Reto Final\n\nCombínalo todo: Lee de archivo, lee de consola, convierte ambas, súmalas, ¡e imprime el resultado!",
        "EUS": "## 5. Azken Erronka\n\nKonbinatu dena: Irakurri fitxategitik, irakurri kontsolatik, bihurtu biak, batu, eta inprimatu emaitza!"
      }
    }
  ]
}